export type UserType = {
    username: string,
    hashedPassword: string,
    fullName: string,
    phoneNumber: string,
    UID: string,
    email: string,
    upiId: string,
    coursesPurchased: string[]
}