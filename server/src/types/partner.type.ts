export type PartnerType = {
    username: string,
    hashedPassword: string,
    fullName: string,
    phoneNumber: string,
    UID: string,
    email: string,
    upiId: string,
    rating: number,
    coursesSelling: string[],
}